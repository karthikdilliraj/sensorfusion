#ifndef UPDATE_STUCK_SENSOR_LIST_H
#define UPDATE_STUCK_SENSOR_LIST_H

#if 0
struct sensorList
{
    int number_of_sensors;
    int *values;
    int timestamp;
};

int update_stuck_sensor_list(struct sensorList sensor);
#endif

#endif
